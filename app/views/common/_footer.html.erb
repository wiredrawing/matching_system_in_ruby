<% if (@current_user != nil) then%>
  <!-- ユーザーログイン中メニュー -->
  <div class="footer_menu">
    <div class="footer_menu_in">
      <div class="footer_menu_icon">
        <a href="<%= mypage_url() %>">
          <img src="/images/footer_menu1.png">
        </a>
        <span>マイページ</span>
      </div>
      <div class="footer_menu_icon">
        <% if @uncheck_messages != nil && @uncheck_messages.length > 0 %>
          <span class="pochi"><span class="pochi_in"></span></span>
        <% end %>
        <a href="<%= messages_url() %>"><img src="/images/footer_menu2.png"></a>
        <span>メッセージ</span>
      </div>
      <div class="footer_menu_icon">
        <% if @uncheck_notices != nil && @uncheck_notices.length > 0 then%>
          <span class="pochi"><span class="pochi_in"></span></span>
        <% end %>
        <a href="<%= notices_url %>"><img src="/images/footer_menu3.png"></a>
        <span>お知らせ</span>
      </div>
      <div class="footer_menu_icon">
        <% if @uncheck_footprints != nil && @uncheck_footprints.length > 0 %>
          <span class="pochi"><span class="pochi_in"></span></span>
        <% end %>
        <a href="<%= mypage_footprints_url() %>"><img src="/images/footer_menu4.png"></a>
        <span>足跡</span>
      </div>
    </div>
  </div>
  <!--footer_menu-->
<% end %>
<footer>
</footer>
<script type="text/javascript">
  $(document).ready(function() {
      $(".drawer").drawer();
  });
  $(".backk").on("click", function (e) {
      e.preventDefault();
      history.back();
  });
  $(".button_to_back_previous_page").on("click", function (e) {
      e.preventDefault();
      history.back();
  })
</script>
</body>
</html>
